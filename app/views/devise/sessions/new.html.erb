<main class="form-signin">
<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>


    <h1 class="text-center loginhead text-primary">LINK</h1>

    <div class="form-floating">
      <%= f.text_field :login, :class => "form-control", :id => "floatingInput", :placeholder => "name@example.com"  %>
      <!-- <input type="email" class="form-control" id="floatingInput" placeholder="name@example.com"> -->
      <label for="floatingInput">Emailまたはユーザー名</label>
    </div>
    <div class="form-floating">
      <%= f.password_field :password, autocomplete: "current-password", :class => "form-control", :id => "floatingPassword", :placeholder => "パスワード" %>
      <!-- <input type="password" class="form-control" id="floatingPassword" placeholder="パスワード"> -->
      <label for="floatingPassword">パスワード</label>
    </div>


    <% if devise_mapping.rememberable? %>
    <div class="form-check mb-3">
      <label>
          <%= f.check_box :remember_me %>
        <!-- <input type="checkbox" value="remember-me">  -->
        状態を記憶する
      </label>
    </div>
    <% end %>

    <%= f.submit "ログイン", class: "w-100 btn btn-lg btn-primary" %>
    <!-- <button class="w-100 btn btn-lg btn-primary" type="submit">サインイン</button> -->


<% end %>
<%= render "devise/shared/links" %>
</main>
